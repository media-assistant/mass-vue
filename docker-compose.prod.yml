version: "3.7"

services:
  mass-vue:
    container_name: mass-vue
    environment:
      - PUID=1000
      - PGID=1000
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - 5000:80
